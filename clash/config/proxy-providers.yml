x-proxy-providers: &x-proxy-providers
  type: http
  interval: 3600
  health-check:
    enable: true
    url: https://cp.cloudflare.com
    interval: 300

proxy-providers:
  # example:
  #   <<: *x-proxy-providers
  #   url: "https://example.com/config.yaml"